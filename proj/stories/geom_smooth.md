# Geom smooth

`GEOM SMOOTH` plots a regression line. Should support different models:
* loess
* linear model

This should also (have the option to) display confidence interval around the smooth as a band around the line.
