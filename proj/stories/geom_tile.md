As an analyst with two categorical variables I want to produce a tile-plot / heatmap showing a matrix of the two variables with cell fill color representing the data.

If no `fill` aesthetic is passed, default to stat `count`; otherwise default to stat `sum`.

Geom tile creates a heatmap using two categorical variables and the count() statistic.
