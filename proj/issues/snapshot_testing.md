I'm looking to build a regression testing framework to help catch bugs as this project gets larger.

The idea would be to use the `--output` render target to produce a file that can be checked into source control and diffed in CI.
